apiVersion: orbixal.ai/v1
kind: agent

metadata:
  name: Orbixal Agent Hello World
  slug: orbixal_agent_hello_world
  version: 1.0.4
  description: Reads a JSON name and writes a JSON greeting.
  tags:
    - example
    - hello-world
    - job

spec:
  build:
    context: "."
    dockerfile: Dockerfile

  runtime:
    type: container
    supported_modes:
      - job
    default_mode: job
    job:
      execution:
        command:
          - python
          - /app/main.py
    timeouts:
      startupSeconds: 30
      requestSeconds: 60

  io:
    contract: json-v1
    input_schema:
      type: object
      additionalProperties: false
      required:
        - name
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
    output_schema:
      type: object
      additionalProperties: false
      required:
        - greeting
      properties:
        greeting:
          type: string
    conformance_input:
      name: Orbixal
